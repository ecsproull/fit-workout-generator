# React + Vite

This is a user interface for entering JSON markup that represents a swim workout and produce a FIT Workout file to be used on a garmin watch. 

Required to run? VS Code and and an AI source. I used ChatGPT for all of my testing. Since this runs in a browser, any OS should do as long as you can run VS Code on that OS.

Workflow:
